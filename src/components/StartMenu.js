import Button from 'react-bootstrap/Button';

export default function StartMenu() {
    return(
        <main>
            <h1>OTTOMH</h1>
            <Button variant="primary" type="submit">Create new room</Button>
        </main>
    );
}